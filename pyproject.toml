[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "composio-toolkit-ops-agent"
version = "0.1.0"
description = "Recipe-driven operations control plane for managed auth, Playwright, and gated routes."
readme = "README.md"
requires-python = ">=3.11,<3.12"

[tool.setuptools.packages.find]
include = ["api*", "ops*"]

[tool.pytest.ini_options]
addopts = "-ra"
asyncio_mode = "auto"
pythonpath = ["."]
testpaths = ["tests"]
timeout = 60
timeout_method = "signal"
markers = [
    "live: opt-in test that may call a configured external provider or perform a controlled side effect",
    "browser: real-Chromium integration test against the local test app (must not skip when REQUIRE_REAL_BROWSER_TESTS=1)",
]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B", "UP"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_configs = true
exclude = ["^fixtures/"]
