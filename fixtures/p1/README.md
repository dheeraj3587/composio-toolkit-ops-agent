# P1 fixtures

Use only minimal records derived from public, non-secret P1 contract fields. Prefer constructing test
records in code. Never copy P1 application code, report assets, reasoning logs, hand-check artifacts,
or unrelated rows here. The provenance-locked canonical snapshot lives under `data/p1/` and must not
be edited by tests.
